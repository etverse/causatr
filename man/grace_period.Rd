% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glmtp_interventions.R
\name{grace_period}
\alias{grace_period}
\title{Grace-period (delayed-initiation) natural-history treatment policy}
\usage{
grace_period(window = 1L, budget = 1024L)
}
\arguments{
\item{window}{Non-negative integer. Number of periods by which initiation is
delayed. \code{window = 0} makes initiation absorbing at the natural initiation
time (a no-delay reference); \code{window = 1} is the canonical one-period
delay from the paper.}

\item{budget}{Positive integer. Maximum allowed worst-step natural-history
enumeration \eqn{|\mathcal{A}|^{\tau-1}} before the augmentation is deemed
intractable (see \code{\link[=glmtp_check_tractable]{glmtp_check_tractable()}}). Default \code{1024}.}
}
\value{
A \code{causatr_glmtp} object (also inheriting \code{causatr_intervention})
carrying the policy closure.
}
\description{
Creates a natural-history modified treatment policy (G-LMTP) that \strong{delays
treatment initiation by \code{window} periods}. Under the policy a patient
initiates treatment exactly \code{window} periods after they would have initiated
under their own natural treatment process, and treatment is absorbing once
started (Diaz, Williams, Morzywolek & Rudolph 2026; the policy used in their
simulation study, Section 6). For binary treatment coded \code{1} = on treatment,
the intervened value is
\deqn{A^d_t = \mathbf{1}\{\text{natural initiation occurred by period } t -
\text{window}\},}
which depends on the \strong{natural-value history of treatment}, not just the
contemporaneous natural value.

This is \strong{not} expressible as a \code{\link[=dynamic]{dynamic()}} rule: the standard ICE recursion
conditions on the \emph{observed} lagged treatment, whereas the natural value at
period \eqn{t} under the regime differs from the observed value whenever the
policy has already perturbed the treatment trajectory (treatment-state
feedback). The estimate is produced by the augmented-data sequential
regression and is only valid for \strong{longitudinal g-computation}
(\code{estimator = "gcomp"}) on a \strong{discrete} treatment.
}
\examples{
\dontrun{
fit <- causat(long_data, outcome = "Y", treatment = "A",
              confounders = ~ L0, confounders_tv = ~ L,
              id = "id", time = "t", estimator = "gcomp")
contrast(fit, interventions = list(
  delay1 = grace_period(1),
  natural = NULL
), ci_method = "bootstrap")
}

}
\references{
Diaz I, Williams NT, Morzywolek P, Rudolph KE (2026). Modified treatment
policies that depend on the natural history of treatment. arXiv:2605.24167.
}
\seealso{
\code{\link[=carry_forward]{carry_forward()}}, \code{\link[=contrast]{contrast()}}, \code{\link[=dynamic]{dynamic()}}

Other glmtp: 
\code{\link{carry_forward}()}
}
\concept{glmtp}
