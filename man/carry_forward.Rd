% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glmtp_interventions.R
\name{carry_forward}
\alias{carry_forward}
\title{Carry-forward (LOCF) natural-history treatment policy}
\usage{
carry_forward(seed = "baseline", budget = 1024L)
}
\arguments{
\item{seed}{Either the string \code{"baseline"} (default; carry each
individual's natural baseline value \eqn{A_1} forward) or a single
numeric value (carry that fixed value forward at every period).}

\item{budget}{Positive integer. Worst-step enumeration budget, as in
\code{\link[=grace_period]{grace_period()}}. Default \code{1024}.}
}
\value{
A \code{causatr_glmtp} object (also inheriting \code{causatr_intervention}).
}
\description{
Creates a natural-history modified treatment policy that \strong{carries the
seed treatment value forward} at every period:
\eqn{A^d_t = A^d_{t-1} = \dots = A^d_1}. With the default
\code{seed = "baseline"} the seed is each individual's own baseline natural
treatment value \eqn{A_1}, so the policy holds everyone at their
baseline level for the whole follow-up. With a numeric \code{seed} the policy
sets every period to that fixed value (reducing to a \code{\link[=static]{static()}} regime).

This is the degenerate (limiting) member of the G-LMTP family: because the
intervened value depends only on the \strong{baseline} natural value, it equals a
standard ICE regime that sets current treatment to baseline. It is provided
as a convenient constructor for the common last-observation-carried-forward
policy and routes through the same augmented engine as \code{\link[=grace_period]{grace_period()}} so
the two share a code path.
}
\examples{
\dontrun{
fit <- causat(long_data, outcome = "Y", treatment = "A",
              confounders = ~ L0, confounders_tv = ~ L,
              id = "id", time = "t", estimator = "gcomp")
contrast(fit, interventions = list(
  locf = carry_forward(),
  natural = NULL
), ci_method = "bootstrap")
}

}
\references{
Diaz I, Williams NT, Morzywolek P, Rudolph KE (2026). Modified treatment
policies that depend on the natural history of treatment. arXiv:2605.24167.
}
\seealso{
\code{\link[=grace_period]{grace_period()}}, \code{\link[=static]{static()}}, \code{\link[=contrast]{contrast()}}

Other glmtp: 
\code{\link{cap_escalation}()},
\code{\link{grace_period}()}
}
\concept{glmtp}
