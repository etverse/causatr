% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy.causatr_result}
\alias{tidy.causatr_result}
\title{Tidy a causatr result}
\usage{
\method{tidy}{causatr_result}(
  x,
  which = c("contrasts", "means", "all"),
  conf.int = TRUE,
  conf.level = 0.95,
  ...
)
}
\arguments{
\item{x}{A \code{causatr_result} object.}

\item{which}{Character. What to tidy: \code{"contrasts"} (default),
\code{"means"}, or \code{"all"} (both).}

\item{conf.int}{Logical. Include confidence interval columns? Default
\code{TRUE}.}

\item{conf.level}{Numeric. Confidence level for intervals. Default \code{0.95}.}

\item{...}{Currently unused.}
}
\value{
A data.frame with columns \code{term}, \code{estimate}, \code{std.error},
\code{conf.low}, \code{conf.high}, and \code{type} (either \code{"mean"} or \code{"contrast"}).
}
\description{
Returns a tidy data frame of intervention means and/or pairwise contrasts
from a \code{causatr_result} object, compatible with the
\href{https://broom.tidymodels.org/}{broom} ecosystem.
}
\examples{
\dontrun{
result <- contrast(fit, interventions = list(a1 = static(1), a0 = static(0)))
tidy(result)
tidy(result, which = "means")
tidy(result, conf.level = 0.99)
}

}
\seealso{
\code{\link[=coef.causatr_result]{coef.causatr_result()}}, \code{\link[=confint.causatr_result]{confint.causatr_result()}}
}
