% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causat.R
\name{causat}
\alias{causat}
\title{Fit a causal model}
\usage{
causat(
  data,
  outcome,
  treatment,
  confounders,
  method = c("gcomp", "ipw", "matching"),
  family = "gaussian",
  id = NULL,
  time = NULL,
  censoring = NULL,
  weights = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame or data.table.}

\item{outcome}{Character. Name of the outcome variable.}

\item{treatment}{Character. Name of the treatment variable.}

\item{confounders}{A one-sided formula specifying confounders,
e.g. \code{~ L1 + L2}. Interactions and transformations are allowed,
e.g. \code{~ L1 * L2 + splines::ns(age, 4)}.}

\item{method}{Character. Estimation method: \code{"gcomp"} (default), \code{"ipw"},
or \code{"matching"}. IPW requires the \code{WeightIt} package; matching requires
the \code{MatchIt} package.}

\item{family}{Character or family object. The outcome model family for
\code{"gcomp"} (e.g. \code{"gaussian"}, \code{"binomial"}). Passed to \code{glm()} or
\code{mgcv::gam()}. Ignored for \code{"ipw"} and \code{"matching"}.}

\item{id}{Character or \code{NULL}. Name of the individual ID variable. Must be
provided together with \code{time} for longitudinal data.}

\item{time}{Character or \code{NULL}. Name of the time variable. Must be
provided together with \code{id} for longitudinal data.}

\item{censoring}{Character or \code{NULL}. Name of the censoring indicator
variable (1 = censored, 0 = uncensored).}

\item{weights}{Numeric vector or \code{NULL}. Pre-computed observation weights
(e.g. survey weights or externally computed IPCW). For \code{"gcomp"},
passed to \code{glm()}. For \code{"ipw"}, multiplied with the estimated propensity
weights.}

\item{...}{Additional arguments passed to the underlying estimation
function: \code{WeightIt::weightit()} for \code{method = "ipw"} (e.g.
\code{estimand = "ATE"}, \code{method = "glm"}); \code{MatchIt::matchit()} for
\code{method = "matching"} (e.g. \code{method = "nearest"}, \code{ratio = 1}).}
}
\value{
A \code{causatr_fit} object with slots:
\describe{
\item{\code{model}}{Fitted model object(s): \code{glm}/\code{gam} for \code{"gcomp"};
\code{NULL} for \code{"ipw"} and \code{"matching"} (weights/matched data are
stored in \code{weights_obj} / \code{match_obj} instead).}
\item{\code{data}}{data.table used for fitting.}
\item{\code{treatment}, \code{outcome}, \code{confounders}, \code{family}}{Model spec.}
\item{\code{method}}{\code{"gcomp"}, \code{"ipw"}, or \code{"matching"}.}
\item{\code{type}}{\code{"point"} or \code{"longitudinal"}.}
\item{\code{id}, \code{time}, \code{censoring}}{Longitudinal identifiers.}
\item{\code{weights_obj}}{\code{weightit} object (IPW only).}
\item{\code{match_obj}}{\code{matchit} object (matching only).}
\item{\code{call}}{The original call.}
\item{\code{details}}{Internal diagnostics list.}
}
}
\description{
Prepares the causal estimation pipeline for a given method. For
\code{"gcomp"}, fits the conditional outcome model E[Y | A, L] that will be
used by \code{\link[=contrast]{contrast()}} for standardisation. For \code{"ipw"}, estimates
propensity-score-based weights (via \code{WeightIt::weightit()}) that will be
used for weighted estimation in \code{\link[=contrast]{contrast()}}. For \code{"matching"}, creates
matched sets (via \code{MatchIt::matchit()}) that will be used for matched
estimation in \code{\link[=contrast]{contrast()}}.

For longitudinal data (\code{id} and \code{time} provided), \code{"gcomp"} uses ICE
g-computation (Zivich et al., 2024): outcome models are fitted at each
time point via backward iteration.
}
\details{
\subsection{G-computation (\code{method = "gcomp"})}{

Fits E[Y | A, L] using \code{glm()} (or \code{mgcv::gam()} if the formula
contains \code{s()} or \code{te()} terms). \code{\link[=contrast]{contrast()}} standardises over the
observed covariate distribution to obtain E[Y^a] = (1/n) Σᵢ Ê[Y | A=a,
Lᵢ] under each intervention. Supports any intervention type: static,
dynamic, shift, threshold, scale, IPSI.

For longitudinal data, uses ICE g-computation (Zivich et al., 2024):
outcome models are fitted one per time point via backward iteration.
The resulting pseudo-outcomes carry the intervention-specific causal
information forward.
}

\subsection{IPW (\code{method = "ipw"})}{

Calls \code{WeightIt::weightit()} to estimate propensity-score-based weights
for the desired \code{estimand} (ATE by default; pass \code{estimand = "ATT"} via
\code{...}), then fits a weighted outcome model via \code{WeightIt::glm_weightit()}.
\code{\link[=contrast]{contrast()}} standardises predictions from that model under each
intervention. Variance via M-estimation accounts for weight estimation
uncertainty. Supports binary, categorical, and continuous treatments
(anything \code{WeightIt} supports). Only \code{static()} interventions are
supported, because the weights were estimated under the observed treatment
distribution; re-estimating under a different regime (shift, dynamic, etc.)
requires re-calling \code{causat()}.
}

\subsection{Matching (\code{method = "matching"})}{

Calls \code{MatchIt::matchit()} to create matched sets (binary, categorical,
and continuous treatments are supported — see \code{MatchIt} documentation),
then fits the outcome model on the matched sample. \code{\link[=contrast]{contrast()}} standardises
predictions from that model. Variance via cluster-robust sandwich SE on
matched pairs. Only \code{static()} interventions are supported for the same
reason as IPW.
}

\subsection{Identifiability assumptions}{

All methods assume: (1) exchangeability (no unmeasured confounding given
L), (2) positivity (every individual has positive probability of each
treatment value given L), (3) consistency (the observed outcome under the
observed treatment equals the potential outcome). Positivity is checked
automatically and a warning is issued if near-violations are detected.
}
}
\examples{
\dontrun{
data("nhefs", package = "causatr")

# Point treatment, g-computation (default)
fit <- causat(
  nhefs,
  outcome = "wt82_71",
  treatment = "qsmk",
  confounders = ~ sex + age + race + education +
    smokeintensity + smokeyrs + exercise + active + wt71
)

# IPW (requires WeightIt)
fit_ipw <- causat(
  nhefs,
  outcome = "wt82_71",
  treatment = "qsmk",
  confounders = ~ sex + age + race + education +
    smokeintensity + smokeyrs + exercise + active + wt71,
  method = "ipw",
  estimand = "ATE"
)

# Matching (requires MatchIt)
fit_match <- causat(
  nhefs,
  outcome = "wt82_71",
  treatment = "qsmk",
  confounders = ~ sex + age + race + education +
    smokeintensity + smokeyrs + exercise + active + wt71,
  method = "matching",
  match_method = "nearest"
)
}

}
\references{
Hernán MA, Robins JM (2025). \emph{Causal Inference: What If}. Chapman &
Hall/CRC. Chapters 12 (IPW), 13 (g-formula), 15 (matching), 21 (ICE).

Zivich PN, Ross RK, Shook-Sa BE, Cole SR, Edwards JK (2024). Empirical
sandwich variance estimator for iterated conditional expectation
g-computation. \emph{Statistics in Medicine} 43:5562–5572.
}
\seealso{
\code{\link[=contrast]{contrast()}}, \code{\link[=diagnose]{diagnose()}}, \code{\link[=causat_survival]{causat_survival()}},
\code{\link[=static]{static()}}, \code{\link[data.table:shift]{data.table::shift()}}, \code{\link[=dynamic]{dynamic()}}
}
