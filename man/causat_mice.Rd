% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causat_mice.R
\name{causat_mice}
\alias{causat_mice}
\title{Pool causal estimates across multiply-imputed datasets}
\usage{
causat_mice(
  imp,
  outcome,
  treatment,
  confounders = NULL,
  interventions = NULL,
  estimator = "gcomp",
  family = "gaussian",
  estimand = "ATE",
  type = "difference",
  ci_method = "sandwich",
  conf_level = 0.95,
  by = NULL,
  pool_method = c("rubin", "boot_mi"),
  B = 200L,
  M = 2L,
  parallel = c("no", "future"),
  seed = NULL,
  ...
)
}
\arguments{
\item{imp}{A \code{mids} object returned by \code{mice::mice()}.}

\item{outcome}{Character scalar naming the outcome column. Passed to
\code{\link[=causat]{causat()}}.}

\item{treatment}{Character scalar (or vector for multivariate treatment)
naming the treatment column(s). Passed to \code{\link[=causat]{causat()}}.}

\item{confounders}{A one-sided formula of baseline confounders, or \code{NULL}
when per-component formulas are supplied through \code{...}. Passed to
\code{\link[=causat]{causat()}}.}

\item{interventions}{A named list of intervention objects (e.g.
\code{list(a1 = static(1), a0 = static(0))}). Passed to \code{\link[=contrast]{contrast()}}. Leave
\code{NULL} for \code{estimator = "snm"}, whose estimand is the blip parameter
itself and which rejects an \code{interventions} argument.}

\item{estimator}{Character causal estimator: \code{"gcomp"} (default), \code{"ipw"},
\code{"aipw"}, \code{"matching"}, or \code{"snm"}. Passed to \code{\link[=causat]{causat()}}.}

\item{family}{Outcome family (character or family object). Passed to
\code{\link[=causat]{causat()}}.}

\item{estimand}{Character estimand (\code{"ATE"}, \code{"ATT"}, \code{"ATC"}). Passed to
\code{\link[=causat]{causat()}}.}

\item{type}{Character contrast scale: \code{"difference"} (default), \code{"ratio"},
or \code{"or"}. Passed to \code{\link[=contrast]{contrast()}}.}

\item{ci_method}{Character within-imputation variance method, \code{"sandwich"}
(default) or \code{"bootstrap"}, used for each per-imputation \code{\link[=contrast]{contrast()}}
call. The \emph{pooled} variance is governed by \code{pool_method}, not this
argument.}

\item{conf_level}{Numeric confidence level for the pooled intervals.
Default \code{0.95}.}

\item{by}{Optional one-sided formula or character naming a baseline
stratifier. Pooling is applied per \code{by}-stratum row independently. Passed
to \code{\link[=contrast]{contrast()}}.}

\item{pool_method}{Character pooling strategy. \code{"rubin"} (default) applies
Rubin's rules to the per-imputation sandwich variances. \code{"boot_mi"} uses
von Hippel's bootstrap-then-impute two-stage variance, valid under
uncongeniality. See Details.}

\item{B}{Integer number of bootstrap resamples for \code{pool_method = "boot_mi"}. Default \code{200}. Ignored for \code{"rubin"}.}

\item{M}{Integer number of imputations per bootstrap resample for
\code{pool_method = "boot_mi"}. Default \code{2} (von Hippel's efficient variant).
Ignored for \code{"rubin"}.}

\item{parallel}{Character parallel backend forwarded to the Boot MI engine:
\code{"no"} (default) or \code{"future"} (uses \code{future.apply::future_lapply()}).}

\item{seed}{Optional integer seed. For \code{pool_method = "boot_mi"} it seeds
the bootstrap-and-impute loop reproducibly.}

\item{...}{Additional arguments forwarded to \code{\link[=causat]{causat()}} (e.g. \code{id},
\code{time}, \code{confounders_tv}, \code{censoring}, \code{ipcw}, \code{confounders_outcome},
\code{propensity_model_fn}, \code{model_fn}).}
}
\value{
A \code{causatr_result} with pooled estimates, standard errors, and
confidence intervals. \code{ci_method} is \code{"rubin"} or \code{"boot_mi"}. The
per-row pooling diagnostics are attached as the \code{"mi_details"} attribute.
}
\description{
Fits a causal model with \code{\link[=causat]{causat()}} and computes a causal \code{\link[=contrast]{contrast()}} on
every completed dataset stored in a \code{mice} \code{mids} object, then pools the
per-imputation estimates into a single \code{causatr_result}. This is the
analysis step of a multiple-imputation (MI) workflow: the user imputes
missing covariates and/or treatment upstream with \code{mice::mice()}, and
\code{causat_mice()} propagates the imputation uncertainty into the causal
estimate and its standard error.

Multiple imputation is the right tool for \strong{missing covariates (L)} or
\strong{missing treatment (A)} under a missing-at-random mechanism. Missing
\strong{outcomes (Y)} are handled by inverse-probability-of-censoring weighting
(\code{ipcw = TRUE}) or complete-case analysis, not by imputing Y; however Y
\emph{should} be a predictor in the upstream imputation model.
}
\details{
\subsection{Rubin's rules (\code{pool_method = "rubin"})}{

Let \eqn{\hat{Q}_i} and \eqn{U_i} be the estimate and variance from
imputation \eqn{i}. The pooled estimate is \eqn{\bar{Q} = m^{-1}\sum_i
\hat{Q}_i} and the total variance is \eqn{T = \bar{U} + (1 + 1/m) B} with
within variance \eqn{\bar{U} = m^{-1}\sum_i U_i} and between variance
\eqn{B = (m-1)^{-1}\sum_i (\hat{Q}_i - \bar{Q})^2}. Confidence intervals use
Barnard-Rubin degrees of freedom.
}

\subsection{Congeniality}{

Causal estimands are typically \emph{uncongenial} with the \code{mice} imputation
model (the estimand is a functional of the outcome/treatment model under
intervention, not a parameter of the imputation model). Under uncongeniality
Rubin's variance can be biased in \emph{either} direction depending on the
situation -- conservative for some kinds of uncongeniality (Meng 1994), but
anticonservative in others (Bartlett & Hughes 2020). \code{pool_method = "boot_mi"} sidesteps Rubin's variance decomposition with a resampling
variance that attains nominal coverage \strong{provided the point estimator stays
consistent}: a bootstrap corrects the variance, not bias in the estimate
itself. Always include the outcome, treatment, all confounders, and any
effect modifiers as predictors in the upstream \code{mice()} call -- omitting a
key predictor (e.g. the outcome) misspecifies the imputation, which can
\emph{bias} the causal estimate and so defeat both pooling rules.
\code{causat_mice()} warns when an analysis variable is absent or unused.
}

\subsection{What this does not do}{

It does not perform the imputation (call \code{mice::mice()} first), impute the
outcome, handle MNAR mechanisms, or pool omnibus tests across contrasts.
}
}
\examples{
if (requireNamespace("mice", quietly = TRUE)) {
  set.seed(1)
  n <- 400
  L <- rnorm(n)
  A <- rbinom(n, 1, plogis(0.5 * L))
  Y <- 2 + 3 * A + 1.5 * L + rnorm(n)
  # L missing-at-random on the (observed) treatment.
  L[rbinom(n, 1, plogis(-1 + 0.8 * A)) == 0] <- NA
  dat <- data.frame(Y = Y, A = A, L = L)

  imp <- mice::mice(dat, m = 5, printFlag = FALSE)
  res <- causat_mice(
    imp,
    outcome = "Y",
    treatment = "A",
    confounders = ~L,
    interventions = list(a1 = static(1), a0 = static(0)),
    estimator = "gcomp"
  )
  summary(res)
}

}
\references{
Rubin DB (1987). \emph{Multiple Imputation for Nonresponse in Surveys}. Wiley.

van Buuren S, Groothuis-Oudshoorn K (2011). mice: Multivariate Imputation
by Chained Equations in R. \emph{Journal of Statistical Software} 45(3):1-67.

von Hippel PT (2020). How many imputations do you need? \emph{Sociological
Methods & Research} 49(3):699-718.

Meng XL (1994). Multiple-imputation inferences with uncongenial sources of
input. \emph{Statistical Science} 9(4):538-558.

Bartlett JW, Hughes RA (2020). Bootstrap inference for multiple imputation
under uncongeniality and misspecification. \emph{Statistical Methods in Medical
Research} 29(12):3533-3546.
}
\seealso{
\code{\link[=causat]{causat()}}, \code{\link[=contrast]{contrast()}}
}
