% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causat_mice.R
\name{causat_mice}
\alias{causat_mice}
\title{Fit causal models across multiply-imputed datasets}
\usage{
causat_mice(
  imp,
  outcome,
  treatment,
  confounders,
  interventions,
  method = "gcomp",
  family = "gaussian",
  estimand = "ATE",
  type = "difference",
  ci_method = "sandwich",
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{imp}{A \code{mids} object returned by \code{mice::mice()}.}

\item{outcome}{Character. Passed to \code{\link[=causat]{causat()}}.}

\item{treatment}{Character. Passed to \code{\link[=causat]{causat()}}.}

\item{confounders}{A one-sided formula. Passed to \code{\link[=causat]{causat()}}.}

\item{interventions}{A named list of interventions. Passed to \code{\link[=contrast]{contrast()}}.}

\item{method}{Character. Passed to \code{\link[=causat]{causat()}}. Default \code{"gcomp"}.}

\item{family}{Character or family object. Passed to \code{\link[=causat]{causat()}}.}

\item{estimand}{Character. Passed to \code{\link[=causat]{causat()}}.}

\item{type}{Character. Contrast scale. Passed to \code{\link[=contrast]{contrast()}}.}

\item{ci_method}{Character. Variance method. Passed to \code{\link[=contrast]{contrast()}}.}

\item{conf_level}{Numeric. Confidence level. Default \code{0.95}.}

\item{...}{Additional arguments passed to \code{\link[=causat]{causat()}}.}
}
\value{
A \code{causatr_result} with pooled estimates and variances following
Rubin's rules.
}
\description{
Applies \code{\link[=causat]{causat()}} and \code{\link[=contrast]{contrast()}} across all imputed datasets in a
\code{mids} object (from \code{mice::mice()}), then pools point estimates and
variances using Rubin's rules. This handles missing treatment values (or
any other missing data) via multiple imputation.

Rubin's rules pool m point estimates and their within-imputation variances:
\itemize{
\item \strong{Pooled estimate}: mean of per-imputation estimates.
\item \strong{Total variance}: within-imputation variance + between-imputation
variance + between-imputation correction.
\item \strong{CIs}: normal approximation on the pooled estimate.
}
}
\details{
\subsection{Rubin's rules}{

Let \verb{Q̂_i} be the estimate from imputation \code{i} and \code{U_i} its variance.
The pooled estimate is \verb{Q̄ = (1/m) Σ Q̂_i}. The total variance is:

\if{html}{\out{<div class="sourceCode">}}\preformatted{T = Ū + B + B/m
}\if{html}{\out{</div>}}

where \verb{Ū = (1/m) Σ U_i} (within-imputation variance) and
\verb{B = (1/(m-1)) Σ (Q̂_i - Q̄)²} (between-imputation variance).

Degrees of freedom follow the standard Barnard–Rubin approximation.
}
}
\examples{
\dontrun{
library(mice)

# Step 1: impute
imp <- mice(data, m = 20, method = "pmm")

# Step 2: fit + contrast across imputations
result <- causat_mice(
  imp,
  outcome = "Y",
  treatment = "A",
  confounders = ~ L1 + L2,
  interventions = list(treat_all = static(1), treat_none = static(0)),
  type = "difference"
)
}

}
\references{
Rubin DB (1987). \emph{Multiple Imputation for Nonresponse in Surveys}.
Wiley.

van Buuren S, Groothuis-Oudshoorn K (2011). mice: Multivariate Imputation
by Chained Equations in R. \emph{Journal of Statistical Software} 45(3):1–67.
}
\seealso{
\code{\link[=causat]{causat()}}, \code{\link[=contrast]{contrast()}}
}
