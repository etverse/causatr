% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interventions.R
\name{stochastic}
\alias{stochastic}
\title{Stochastic intervention (user-supplied sampling function)}
\usage{
stochastic(sampler, n_mc = 100L)
}
\arguments{
\item{sampler}{A function with signature \verb{function(data, treatment)}
that returns a vector of treatment values of length \code{nrow(data)}.
\code{data} is the full counterfactual data.table (same as \code{\link[=dynamic]{dynamic()}});
\code{treatment} is the observed treatment vector. Each call must return
an independent random draw from the stochastic policy.}

\item{n_mc}{Positive integer. Number of Monte Carlo draws for the
g-formula integration. Default 100. Larger values reduce MC noise
at the cost of computation time. For sandwich variance, \code{n_mc}
should be large enough that MC noise is negligible relative to
the estimation error (100--500 is typical).}
}
\value{
A \code{causatr_intervention} object.
}
\description{
Creates a stochastic intervention where the counterfactual treatment
for each individual is drawn from a user-supplied distribution that
may depend on the individual's covariates. Each call to \code{sampler}
should return an independent draw.

Only supported under \code{estimator = "gcomp"} (point and longitudinal).
The g-formula evaluates \eqn{E[Y^g]} via Monte Carlo integration: for each
of \code{n_mc} draws, the sampler assigns counterfactual treatments, the
outcome model predicts, and the predictions are averaged across draws.
}
\examples{
# Binary treatment: covariate-dependent randomisation
stochastic(\(data, trt) rbinom(nrow(data), 1, plogis(0.5 + 0.3 * data$L)))

# Continuous treatment: additive random shift
stochastic(\(data, trt) trt + rnorm(length(trt), mean = 0.5, sd = 0.25))

}
\references{
Diaz I, van der Laan MJ (2012). Population intervention causal
effects based on stochastic interventions. \emph{Biometrics} 68:541--549.
}
\seealso{
\code{\link[=static]{static()}}, \code{\link[=shift]{shift()}}, \code{\link[=scale_by]{scale_by()}}, \code{\link[=threshold]{threshold()}}, \code{\link[=dynamic]{dynamic()}},
\code{\link[=ipsi]{ipsi()}}, \code{\link[=contrast]{contrast()}}
}
