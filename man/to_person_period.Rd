% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_person_period.R
\name{to_person_period}
\alias{to_person_period}
\title{Convert wide data to person-period (long) format}
\usage{
to_person_period(
  data,
  id,
  time_varying,
  time_invariant = character(0),
  time_name = "time"
)
}
\arguments{
\item{data}{A data frame or data.table in wide format.}

\item{id}{Character. Name of the individual ID variable.}

\item{time_varying}{Named list specifying how to reshape time-varying
columns. Each element is a character vector of column names for one
variable across time, in time order. For example:
\code{list(A = c("A0", "A1", "A2"), L = c("L0", "L1", "L2"))}.}

\item{time_invariant}{Character vector of column names to carry forward
unchanged (baseline covariates, outcome, etc.).}

\item{time_name}{Character. Name for the new time column. Default \code{"time"}.}
}
\value{
A data.table in person-period format with one row per individual
per time point, sorted by \code{id} and \code{time_name}.
}
\description{
Reshapes individual-level data in wide format (one row per person, with
columns for each time-varying variable at each time point) into long
person-period format (one row per person per time interval), as required
by \code{\link[=causat]{causat()}} for longitudinal analyses and by \code{\link[=causat_survival]{causat_survival()}} for
survival analyses.
}
\examples{
\dontrun{
# Toy wide dataset
wide <- data.table::data.table(
  id  = 1:3,
  sex = c(0, 1, 0),
  A0  = c(1, 0, 1),
  A1  = c(1, 1, 0),
  L0  = c(5, 3, 7),
  L1  = c(4, 6, 8),
  Y   = c(0, 1, 0)
)
long <- to_person_period(
  wide,
  id = "id",
  time_varying = list(A = c("A0", "A1"), L = c("L0", "L1")),
  time_invariant = c("sex", "Y")
)
}

}
\seealso{
\code{\link[=causat]{causat()}}, \code{\link[=causat_survival]{causat_survival()}}
}
