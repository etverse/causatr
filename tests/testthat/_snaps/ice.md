# ICE g-computation is not yet implemented

    Code
      causat(df, outcome = "Y", treatment = "A", confounders = ~L, id = "id", time = "time")
    Condition
      Error in `fit_ice()`:
      ! ICE g-computation for longitudinal treatments is not yet implemented.

