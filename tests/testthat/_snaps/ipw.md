# IPW estimation is not yet implemented

    Code
      causat(df, outcome = "Y", treatment = "A", confounders = ~L, method = "ipw")
    Condition
      Error in `check_pkg()`:
      ! Package 'WeightIt' is required but not installed. Install it with: install.packages('WeightIt')

