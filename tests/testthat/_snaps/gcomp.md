# g-computation for point treatments is not yet implemented

    Code
      causat(df, outcome = "Y", treatment = "A", confounders = ~L, method = "gcomp")
    Condition
      Error in `fit_gcomp_point()`:
      ! G-computation for point treatments is not yet implemented.

